server:
  port: 8083
logging:
  level:
   root: INFO
   
  file: 
   name: logs/notification-service.log
spring:
  application:
    name: notification-service
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: notification-group
      auto-offest-reset: earliest
  
     